spring:
  cloud:
    config:
      name: microservicecloud-config-dept        #需要从服务器上读取的资源名称
      profile: test  #本次访问的配置项
      label: master
      uri: http://127.0.0.1:3344 #通过config服务连接到资源项获取对应的资源